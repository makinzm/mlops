pre-commit:
  parallel: true
  commands:
    ruff-check:
      run: uv run ruff check .
    ruff-format:
      run: uv run ruff format --check .
    mypy:
      run: uv run mypy tests/ src/
    actionlint:
      glob: ".github/workflows/*.yml"
      run: devenv shell -- actionlint {staged_files}
