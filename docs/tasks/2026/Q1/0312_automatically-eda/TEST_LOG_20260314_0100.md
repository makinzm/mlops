# TEST LOG — Refactor — 2026-03-14

## 目的

リファクタリングとして以下の変更を TDD で実施:
1. `PandasAnalyzer` から `GitRepository` 依存を除去 → `commit_hash: str` を直接受取る
2. `PolarsAnalyzer` 追加（polars ネイティブで read/stats、常に .parquet 出力）
3. README.md にスキーマテーブルを追加

## RED フェーズのエラーログ

### test_pandas_analyzer.py — 新インターフェースで 18 テスト失敗

```
AttributeError: 'str' object has no attribute 'get_commit_hash'
```

`_make_analyzer` を `PandasAnalyzer(cfg, commit_hash="deadbeef")` に変更したため、
現実装 (`self.git_repo.get_commit_hash()`) が文字列に対してメソッドを呼んでエラー。

### test_polars_analyzer.py — PolarsAnalyzer 未実装

```
ModuleNotFoundError: No module named 'src.infrastructure.analyzer.polars_analyzer'
```

## 次のステップ（GREEN）

1. `PandasAnalyzer.__init__(cfg, commit_hash: str)` に変更し、git 依存を排除
2. `_write_readme` にスキーマテーブルを追加
3. `PolarsAnalyzer` を実装
4. `main.py` の `_resolve_analyzer` を `commit_hash` 注入 + `analyzer_type` 切替に更新
5. `conf/usecase/automatically_eda.yaml` に `analyzer_type: pandas` 追加
