# TEST LOG — Config Refactor — 2026-03-14

## 目的

1. `analyze: {infra_type: {output_format, steps: [...]}}` 形式への config 変更
2. `output_format` の値を `polars/pandas` → `parquet/csv` に修正（命名が不正確だった）
3. 各アナライザーが `{report_dir}/{ANALYZER_TYPE}/` サブディレクトリに書き出す設計

## RED フェーズのエラーログ

```
41 tests failed

# UseCase テスト — list[DataAnalyzer] に変更
TypeError: AutomaticallyEDAUseCase() takes wrong argument type

# Pandas/Polars アナライザーテスト — __init__ シグネチャ変更
TypeError: PandasAnalyzer.__init__() got unexpected keyword argument 'analyses'
TypeError: PandasAnalyzer.__init__() got unexpected keyword argument 'output_format'

# サブディレクトリテスト — 新規追加
AssertionError: result.report_dir.name == "pandas"

# output_format 値の変更
AssertionError (parquet/csv tests using new format names)
```

## 次のステップ（GREEN）

1. `AutomaticallyEDAUseCase`: `list[DataAnalyzer]` 受取り、`list[EDAResult]` 返却
2. `PandasAnalyzer`: `analyses: list[AnalysisStep]`, `output_format: str` を `__init__` に追加
   - `ANALYZER_TYPE = "pandas"` クラス属性追加
   - `report_dir` に `ANALYZER_TYPE` サブディレクトリを追加
   - `output_format` の値: `"parquet"` or `"csv"`
   - `_parse_analyses()` を削除（呼び出し元で解決済み）
3. `PolarsAnalyzer`: `analyses: list[AnalysisStep]` を `__init__` に追加
   - `ANALYZER_TYPE = "polars"` クラス属性追加
   - `report_dir` に `ANALYZER_TYPE` サブディレクトリを追加
4. `conf/usecase/automatically_eda.yaml`: `analyze` 形式に変更
5. `main.py`: `_resolve_analyzers` で `cfg.analyze` を解析
